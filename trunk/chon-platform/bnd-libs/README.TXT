The purpose of this project is:

1. Enable standard jars code in OSGi envirounment.

2. Register jackrabbit repository in OSGi context


Using maven-bundle-plugin to build valid OSGi bundle from standard jars are become part of core platform.
